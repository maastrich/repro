# yaml-language-server: $schema=https://moonrepo.dev/schemas/tasks.json

tasks: 
  parent:
    command: echo $ENV_VAR
    options:
      cache: false
    env:
      ENV_VAR: "parent"

  child:
    extends: parent
